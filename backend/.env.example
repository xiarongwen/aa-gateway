# AI Gateway Configuration

# Server port (default: 3000)
PORT=3000

# Enable proxy service (default: true)
ENABLE_PROXY=true

# Proxy port (default: 8080)
PROXY_PORT=8080

# JWT secret for authentication
JWT_SECRET=your-secret-key-here

# Log level (debug, info, warn, error)
RUST_LOG=info
